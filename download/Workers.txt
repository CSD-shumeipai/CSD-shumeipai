addEventListener(
"fetch",event => {
let url=new URL(event.request.url);
url.hostname="/域名";
url.pathname ="/patht路径";
let request=new Request(url,event.request);
event. respondWith(
fetch(request)
)
}
)
